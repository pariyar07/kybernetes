# v0.1.1 Release Verification

Date: 2026-07-17

## Deterministic Evidence

- All `tests/validate-*.rb` validators pass.
- `actionlint`, workflow YAML parsing, skill-frontmatter checks, repository
  hygiene checks, and runtime-neutrality checks pass.
- Pressure scenario `069-unbounded-information-release.md` is present and
  enforced by `validate-runtime-governance.rb`.
- Scenario `024-over-gating-authorized-simple-task.md` and the runtime-use
  rubric preserve lightweight local work without an information-release gate.
- The governor kernel remains 151 lines versus the 366-line baseline.
- Skill count remains five, with one canonical lifecycle governor.
- `manifest.md`, `reference-index.md`, canonical lifecycle semantics, runtime
  adapter shape, and the `control.md` / `verification.md` trust pair are
  unchanged.

## Runtime Coverage

- The portable runtime matrix records bounded release and local/no-send
  fallback behavior.
- Codex and Claude Code bindings distinguish tool authorization from payload
  release authority and record destination, purpose, payload scope, exclusions,
  handling, approval evidence, and residual risk.
- No destructive or privacy-sensitive external send was performed solely for
  release testing.

## Limitations

- Redaction and payload inspection remain capabilities of the active runtime or
  tool binding; Kybernetes governs the decision and fail-closed fallback but is
  not a data-loss-prevention engine.
- Deterministic live-run artifact validation remains evidence-gated and is not
  introduced by this patch release.
