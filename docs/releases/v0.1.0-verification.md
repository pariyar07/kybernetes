# v0.1.0 Release Verification

Date: 2026-07-17

## Deterministic Evidence

- All `tests/validate-*.rb` validators pass.
- Skill discovery returns five `kybernetes:*` skills.
- Markdown relative-link audit passes.
- No tracked `.superpowers/`, `docs/superpowers/`, `.kybernetes/`, or live
  `.agent-runs/` artifact is admitted.
- Lean governor kernel: 151 lines versus 366 baseline (58.7% reduction).

## Runtime Probes

### Codex

- Local CLI: `codex-cli 0.144.5`.
- Active agent goal tools observed in this release task: `get_goal`,
  `create_goal`, and terminal-only `update_goal`.
- No active agent-callable edit, pause, resume, unblock, clear, delete, or
  replace transition was exposed.
- CLI help exposed session resume but did not prove agent goal lifecycle parity.

### Claude Code

- Local CLI: `2.1.181 (Claude Code)`.
- CLI help exposed session resume, background-agent management, remote control,
  permissions, and multi-agent review surfaces.
- The probe did not expose model-callable goal lifecycle management; user/CLI
  commands and hooks remain distinct from model-callable capability.

### Portable

- `npx skills add . --list` discovered all five skills from the local checkout.
- File-based canonical state, manifest reconstruction, revision checks,
  idempotency, cooperative leases, and manual activation remain the fallback.

## Limitations

- Version/help probes demonstrate installed CLI surfaces, not every plan, app,
  hosted, or session capability.
- No destructive live scheduler, event, notification, or external-effect probe
  was created solely for release testing.
- Transactional distributed coordination remains deferred.
