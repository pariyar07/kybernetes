# v0.1.3 Release Verification

Date: 2026-07-17

## Deterministic Evidence

- All `tests/validate-*.rb` validators pass, including the new skill-naming
  validator.
- `actionlint`, workflow YAML parsing, repository hygiene checks, skill
  inventory checks, and runtime-neutrality checks pass.
- Pressure scenario 074 covers invocation of the retired name and requires
  truthful redirection to the canonical skill.
- The validator rejects both an unsupported compatibility-alias claim and any
  installable skill whose name contains `parallel-coordinator`.

## Boundary Evidence

- `skills/kybernetes-loop-governor/SKILL.md` is unchanged.
- The five-skill inventory and sole canonical governor are unchanged.
- No stub skill, package metadata alias, runtime remap, lifecycle state,
  canonical artifact, or adapter was added.
- The historical bare name remains distinct from the separately gated possible
  future `kybernetes:parallel-coordinator` specialist.

## Limitations

- A third-party runtime may implement its own aliases outside this repository;
  Kybernetes does not claim or validate those mappings.
- Existing private prompts that still use the retired name must be updated to
  invoke `$kybernetes:loop-governor` directly.
