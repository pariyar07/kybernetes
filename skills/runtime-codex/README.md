# runtime-codex

Planned Codex runtime binding.

This folder intentionally does not contain a `SKILL.md` yet. The first Codex behavior should be tested through pressure scenarios before it becomes a runtime skill.

Expected scope:

- Goal mode and `/goal` fallback prompts
- goal checklist references
- subagent contracts
- side chat behavior
- runtime feature detection
- Codex-specific caveats
