# portable-core

Planned fallback binding for unknown or lower-capability agent runtimes.

This folder intentionally does not contain a `SKILL.md` yet.

Expected scope:

- prompt-only launcher
- durable control record fallback
- no-subagent execution mode
- generic verification and HITL prompts
- adapter slots for future runtimes
